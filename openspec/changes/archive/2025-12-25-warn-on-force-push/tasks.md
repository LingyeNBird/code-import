# 实现任务清单

## 1. 实现强制推送警告日志
- [x] 1.1 在 `pkg/git/git.go` 的 `codePush` 函数中,在重试循环前检查 `force` 参数
- [x] 1.2 当 `force` 为 `true` 时,添加 WARN 级别日志,内容包含:仓库路径、强制推送标识、风险提示
- [x] 1.3 确保日志只在循环前记录一次,避免重试时重复输出

## 2. 验证和测试
- [x] 2.1 验证配置 `migrate.force_push=true` 时会输出警告日志
- [x] 2.2 验证 rebase 模式下自动启用强制推送时会输出警告日志
- [x] 2.3 验证普通推送(force=false)时不输出警告日志
- [x] 2.4 检查日志格式和内容是否清晰易懂

## 3. 文档更新
- [x] 3.1 (可选)在 `doc/parameters.md` 中的 `PLUGIN_MIGRATE_FORCE_PUSH` 参数说明中补充"会在迁移日志中输出警告信息"
